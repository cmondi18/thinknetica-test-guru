<h1><%= t('.header') %></h1>

<% if @gists.present? %>
  <table>
    <tr>
      <th><%= t('.question') %></a></th>
      <th><%= t('.URL') %></th>
      <th><%= t('.author') %></th>
    </tr>
    <%= render @gists %>
  </table>
<% else %>
  <h1><%= t('.no_gists') %></h1>
<% end %>